# Developer: Greenshoes Team
# Template file - copy to .env before running the admin panel
# This connects the admin frontend to the backend API which handles:
# - Product management (add, edit, delete, pricing, sales)
# - Inventory updates per size/color
# - Order viewing with addresses and totals
# All part of the "single admin interface" requirement

# Copy this to .env and update with your values
VITE_API_URL=http://localhost:4000/api

# For production (update with your actual backend URL)
# VITE_API_URL=https://your-backend.onrender.com/api